dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install libssl-dev
    - bash ci/rebar3.sh
    - bash ci/kerl.sh
    - bash ci/erlang.sh:
        timeout: 1800
  cache_directories:
    - ~/.cache
    - ~/.kerl
    - ~/bin
    - ~/erlang
test:
  override:
    - make test
